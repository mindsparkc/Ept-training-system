<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sinosoft.admin.category.mapper.CategoryMapper">

    <select id="selectCategory" resultType="com.sinosoft.admin.category.domain.Category">
        ${value}
    </select>

    <insert id="insertCatgory">
        ${value}
    </insert>

    <update id="updateCategory">
        ${value}
    </update>

    <delete id="deleteCategory">
        ${value}
    </delete>
</mapper>
