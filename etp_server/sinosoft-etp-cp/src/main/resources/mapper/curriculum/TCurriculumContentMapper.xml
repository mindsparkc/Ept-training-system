<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sinosoft.etp.cp.curriculum.mapper.TCurriculumContentWithCpMapper">
    <resultMap type="TCurriculumContentWithCp" id="TCurriculumContentResult">
        <result property="id"    column="id"    />
        <result property="cid"    column="cid"    />
        <result property="sid"    column="sid"    />
        <result property="source_type"    column="source_type"    />
        <result property="sort"    column="sort"    />
    </resultMap>

    <sql id="selectTCurriculumContentVo">
        select id, cid, sid, source_type, sort from t_curriculum_content
    </sql>
    <insert id="insertTCurriculumContent" parameterType="TCurriculumContentWithCp" useGeneratedKeys="true" keyProperty="id">
        insert into t_curriculum_content
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="cid != null">cid,</if>
            <if test="sid != null">sid,</if>
            <if test="source_type != null">source_type,</if>
            <if test="sort != null">sort,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="cid != null">#{cid},</if>
            <if test="sid != null">#{sid},</if>
            <if test="source_type != null">#{source_type},</if>
            <if test="sort != null">#{sort},</if>
        </trim>
    </insert>

    <select id="selectTurriculumContentByCid" resultType="tCurriculumContentWithCp">
        SELECT
            tc.id,
            tc.sid,
            tc.source_type,
            info.source_name,
            tc.sort
        FROM t_curriculum_content tc
                 JOIN sourceinfo_v1 info ON tc.sid = info.sid AND tc.source_type = info.type
        WHERE tc.cid = #{id}
        ORDER BY tc.sort
    </select>

    <delete id="deleteTCurriculumContentByCid">
        delete from t_curriculum_content where cid = #{id}
    </delete>
</mapper>
