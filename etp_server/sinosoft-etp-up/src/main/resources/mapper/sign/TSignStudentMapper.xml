<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sinosoft.etp.up.sign.mapper.TSignStudentMapper">

    <resultMap type="TSignStudent" id="TSignStudentResult">
        <result property="id"    column="id"    />
        <result property="hdid"    column="hdid"    />
        <result property="xyid"    column="xyid"    />
        <result property="sfbm"    column="sfbm"    />
    </resultMap>

    <sql id="selectUserVo">
        select u.user_id, u.dept_id, u.user_name, u.nick_name, u.email, u.avatar, u.phonenumber, u.sex, u.status, u.del_flag,
               u.login_ip, u.login_date, u.create_by, u.create_time, u.remark,
               ts.sfbm,ts.hdid,ts.xyid
        from sys_user u

    </sql>
    <select id="selectTSignStudentById" parameterType="Long" resultMap="TSignStudentResult">
        select a.id, a.hdid
        from t_sign_student a
        where a.hdid = #{hdid} and a.xyid = #{xyid}
    </select>

    <insert id="insertTSignStudent" parameterType="TSignStudent" useGeneratedKeys="true" keyProperty="id">
        insert into t_sign_student
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="hdid != null">hdid,</if>
            <if test="xyid != null">xyid,</if>
            <if test="sfbm != null">sfbm,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="hdid != null">#{hdid},</if>
            <if test="xyid != null">#{xyid},</if>
            <if test="sfbm != null">#{sfbm},</if>
        </trim>
    </insert>


</mapper>
