<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sinosoft.etp.up.testpaper.mapper.TTestpaperSubjectMapper">

    <resultMap type="TTestpaperSubject" id="TTestpaperSubjectResult">
        <result property="id"    column="id"    />
        <result property="tkid"    column="tkid"    />
        <result property="sjid"    column="sjid"    />
        <result property="sort"    column="sort"    />
        <result property="type"    column="type"    />
        <result property="score"    column="score"    />
        <result property="delFlag"    column="del_flag"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"    column="update_time"    />
        <result property="remark"    column="remark"    />
    </resultMap>

    <sql id="selectTTestpaperSubjectVo">
        select id, tkid, sjid, sort, type, score, del_flag, create_by, create_time, update_by, update_time, remark from t_testpaper_subject
    </sql>

    <select id="selectTTestpaperSubjectList" parameterType="TTestpaperSubject" resultMap="TTestpaperSubjectResult">
        <include refid="selectTTestpaperSubjectVo"/>
        <where>
            <if test="tkid != null "> and tkid = #{tkid}</if>
            <if test="sjid != null "> and sjid = #{sjid}</if>
            <if test="sort != null "> and sort = #{sort}</if>
            <if test="type != null  and type != ''"> and type = #{type}</if>
            <if test="score != null  and score != ''"> and score = #{score}</if>
        </where>
    </select>


    <select id="getSubjectByPaper" resultType="Map">
        select t.tkid, t.sjid, t.sort,  t.score ,s.lx,
               case when s.lx='1' then '单选'
                    when s.lx='2' then '复选'
                    when s.lx='3' then '判断'
                    when s.lx='4' then '填空'
                    else'主观' end lxbq,
               s.id , s.tg, s.xx, s.da, s.jx, s.sflx, s.sfks
        from t_testpaper_subject t
        join t_subject s on t.tkid = s.id
        where t.sjid = #{sjid}
        order by t.type asc, t.sort asc
    </select>
    <select id="selectScore"  resultType="double">
        select sum(score) as score
        from t_testpaper_subject
        where sjid = #{sjid} and del_flag = '0'
    </select>
</mapper>
