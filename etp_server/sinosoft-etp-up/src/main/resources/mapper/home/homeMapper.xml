<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sinosoft.etp.up.home.mapper.HomeMapper">

    <select id="getZsNum" resultType="java.lang.Integer">
        select count(1) from certificate_grant_record where user_id = #{userId} and status = '99'
    </select>
</mapper>

